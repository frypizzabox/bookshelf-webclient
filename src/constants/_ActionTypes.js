export const users = {
  set: {
    entries: 'USERS.SET.entries',
  },
  upsert: {
    entries: 'USERS.UPSERT.entries',
  },
};

export const books = {
  set: {
    entries: 'BOOKS.SET.entries',
  },
  upsert: {
    entries: 'BOOKS.UPSERT.entries',
  },
};
